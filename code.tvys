fn main() -> i32 {
    let a: i32 = 10;
    let b: i32 = 20;
    
    if (a < b) {
        printInt(a);
    } else {
        printInt(b);
    }
    
    if (a > 5) {
        printInt(42);
    }
    
    if (a > 15) {
        printInt(1);
    } else if (a > 8) {
        printInt(2);
    } else {
        printInt(3);
    }
    
    return 0;
}